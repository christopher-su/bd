<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag 'gl_page', :media => 'screen' %>
  </head>
  <body>

  </body>
  <table>
    <tr class="inq_tab_title">
      <td>Cust Name</td>
      <td>Email</td>
      <td>Phone</td>
      <td>Inquiry Time</td>
    </tr>
    <% @inquiries.each do |i| %>
      <tr class="inq_tab_content">
        <td><%= link_to i.name,i %></td>
        <td><%= i.email %></td>
        <td><%= i.phone %></td>
        <td><%= i.created_at %></td>
        <td><%= link_to "Delete",i,:confirm => "Are you sure you want to delete this record?",:method => :delete %></td>
      </tr>
    <% end %>
  </table>
</html>